# Changelog

## 1.5.1 - 2026-08-02

### 명령어 이름 정리

- 긴 그룹명 `/인공지능`을 `/ai`로 변경
- `/대학생 한눈에`를 `/학교 오늘`로 변경
- 목록형 명령어의 `보기`를 `목록`, `전체`, `확인`으로 구체화
- `/예약 간격`을 `/예약 반복`으로 변경
- `/관리 삭제`를 `/관리 정리`로 변경
- `/관리 타임아웃`·`타임아웃해제`를 `/관리 제한`·`제한해제`로 변경
- `/관리 경고보기`·`경고삭제`를 `/관리 경고목록`·`경고초기화`로 변경
- `/메시지`, `/서버정보`, `/사용자정보`를 `/전송`, `/서버`, `/사용자`로 축약
- `/도움말` 화면을 새 명령어 체계로 다시 작성
- 내부 기능과 기존 데이터베이스 구조는 변경하지 않음

## 1.5.0 - 2026-08-02

### 무료 AI 공급자

- Groq Cloud 무료 API 연동 추가
- `AI_PROVIDER=auto`에서 Groq 키를 우선 사용하고 없으면 Gemini로 전환
- Groq API의 실제 모델 목록을 조회해 일반 대화 모델 자동 선택
- 신규 모델 종료 시 404를 감지하고 다른 사용 가능 모델로 자동 재시도
- `/인공지능 검색`을 `groq/compound-mini` 웹 검색과 출처 응답에 연결
- `/인공지능 사용량`에 공급자, 일반 모델, 검색 모델 표시
- `GROQ_API_KEY`, `GROQ_MODEL`, `GROQ_SEARCH_MODEL`, `AI_MAX_OUTPUT_TOKENS` 설정 추가

## 1.4.3 - 2026-08-02

### 봇 표시 이름

- 기본 표시 이름을 `Milkway Bot`으로 변경
- `BOT_DISPLAY_NAME`을 비우면 Discord의 현재 이름을 코드가 변경하지 않도록 수정

## 1.4.2 - 2026-08-02

### Gemini 모델 선택

- API 키에 실제로 열린 Gemini 모델을 조회해 자동 선택
- 사용 중인 모델이 종료되면 다른 Flash 모델로 자동 교체 후 재시도

## 1.4.0 - 2026-08-01

### 봇 표시 이름

- `BOT_DISPLAY_NAME` 환경변수 추가
- Discord API에서 이름 변경이 거부될 경우 Developer Portal 변경 안내를 로그에 출력
- 일반 서버 역할인 `관리자`는 임의로 변경하지 않음

### 무료 호스팅

- Raspberry Pi 전용 설치 스크립트와 문서 제거
- Oracle Cloud Always Free Ubuntu VM 설치 스크립트 추가
- Oracle VM 부팅 자동 실행과 오류 자동 재시작을 systemd로 구성
- SQLite 데이터가 유지되지 않는 무료 슬립형 웹 호스팅 대신 영구 VM 배포 방식으로 문서 개편

## 1.3.0 - 2026-08-01

### 명령어 단순화

- 모든 주요 슬래시 명령어와 하위 명령어를 한글로 변경
- 명령어 수를 87개에서 59개로 축소
- `/시간표 오늘`, `/시간표 보기` 유지 및 한글 옵션 적용
- `/초대` 정식 Discord 서버 설치 링크 추가
- 개발 서버 동기화 시 과거 전역 영문 명령어 정리

### 제거

- 스터디 모집·참가 기능
- 출석·지각·결석 기능
- 채널 잠금·잠금 해제
- 예약 중지·재개·수정·시간변경·즉시 시험 전송
- 키워드 자동응답
- 채널 요약·학습계획·문장 다듬기·번역·브레인스토밍 AI 명령

### 상시 실행

- Windows 작업 스케줄러 자동 실행 설치·삭제 스크립트 추가
